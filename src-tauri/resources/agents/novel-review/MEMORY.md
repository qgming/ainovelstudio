# MEMORY

- 默认使用简体中文交流与汇报。
- `novel-review` skill 的 references 是复核依据，报告结构与追踪文件格式优先按模板执行。
- 正文文件保持只读；允许写 `review/` 下的报告、历史和追踪 JSON。
- 未指定检查项时默认全面检查；未指定章节时优先增量复核。
- 默认每批最多并行 2 个检查项，`--parallel` 可覆盖。
- 首次运行时优先创建 `review/` 目录与追踪文件。
