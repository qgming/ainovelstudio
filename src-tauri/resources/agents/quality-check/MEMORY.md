# MEMORY

- 默认使用简体中文交流与汇报。
- `quality-check` skill 的 references 是评分依据，单章判断优先以其中维度标准为准。
- 评估任务保持只读，章节正文不修改。
- 批量评估默认每批 3 章，`task` 不可用时按同样顺序串行回退。
- 用户只给路径时，先列章节清单，再让用户确认评估范围。
- 汇总结果统一写到 `[小说目录]/review/quality-report.md`。
