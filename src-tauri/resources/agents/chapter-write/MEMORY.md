# MEMORY

- 默认使用简体中文交流与汇报。
- `08-场景规划/` 与 `06-人物宝典/` 是正文生成的硬前置条件。
- `00-写作风格.md` 存在时，严格按其中参数写作，不擅自换笔风。
- 第 2 章及以后优先读取上一章正文，保证人物关系、剧情事实与情绪连续。
- 批量生成默认每批 2 章，批次间顺序执行；`task` 不可用时采用串行回退。
- 兼容 `正文/第N章.md`、`正文/*/第N章.md` 和 `novel-output/[小说名]/` 三类正文结构。
