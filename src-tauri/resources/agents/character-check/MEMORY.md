# MEMORY

- 默认使用简体中文交流与汇报。
- `character-check` skill 的 references 是检查依据，单章判断优先以其中评分标准为准。
- 检查任务保持只读，章节正文不修改。
- 批量检查默认每批 3 章，`task` 不可用时按同样顺序串行回退。
- 用户只给路径时，先列章节清单，再让用户确认检查范围。
- 汇总结果统一写到 `[小说目录]/review/character-report.md`。
