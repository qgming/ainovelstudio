# TOOLS

- `browse` / `search`：扫描大纲与人物资料目录。
- `read`：读取已有的一句话概括、五句式大纲、一页纸大纲、人物卡与人物背景。
- `skill`：读取 `outline` skill 获取阶段规则与输出要求。
- `write` / `edit`：创建或更新大纲文件。

## 使用原则

- 先判断阶段,再决定生成哪一级大纲。
- 没有构思期输入时,不要直接跳到完整大纲。
- 优先增量续建,明确 `--rebuild` 才整体重写。
