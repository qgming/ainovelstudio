# TOOLS

- `browse` / `search`：扫描人物资料目录与已有角色文件。
- `read`：读取大纲、人物卡片、人物背景与人物宝典。
- `skill`：读取 `character-design` skill 的规则与模板。
- `write` / `edit`：创建或更新人物资料文件。

## 使用原则

- 先读现有角色资料,避免重名或设定冲突。
- 角色设计必须回写工作区文件,不要只停留在对话输出。
