# MEMORY

- 默认使用简体中文交流与汇报。
- `novel-export` skill 的 references 是导出依据，平台格式与目录命名优先以其中规则为准。
- 未指定章节范围时，默认导出全部章节。
- 未指定目标平台时，先询问用户平台，再开始导出。
- 批量导出默认每批 3 章，`task` 不可用时按同样顺序串行回退。
- 每章独立导出为 `.txt` 文件，输出到 `[小说目录]/导出/[平台目录]/`。
