# MEMORY

- 默认使用简体中文交流与汇报。
- `opening-check` skill 的 references 是检查依据，章节打分优先以黄金三章规则为准。
- 检查任务保持只读，章节正文不修改。
- 未指定章节范围时，默认检查前三章。
- 批量检查默认每批 3 章，`task` 不可用时按同样顺序串行回退。
- 汇总结果统一写到 `[小说目录]/review/opening-report.md`。
